{
  "name": "bd-engine-cloud",
  "version": "0.1.0",
  "private": true,
  "description": "Hosted SaaS prototype for BD Engine.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/store.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=22"
  }
}
